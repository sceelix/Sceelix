import Tree from 'react-animated-tree';

This is a simple test
Groups or ungroups actor entities.

![NodeImage_c1722ebb-8bc2-45a1-a146-b3767d611cd1](Nodes/Actor/images/NodeImage_c1722ebb-8bc2-45a1-a146-b3767d611cd1.png)

Tags: Actors, Group, Ungroup

## Parameters

export const Wow = ({children, name, type, description}) => (
  <span style={{whiteSpace: 'normal'}}><b>{name}</b> [<i>{type}</i>]<br/>{description}</span>
);

export const Highlight = ({children}) => (
  <Tree content="Operation [Select]" open>
  <Tree content={<Wow name='Hello' type='Compound' description='Orients the actor so that a given scope axis direction faces a certain direction. For instance, setting the axis direction "X" to face (0,0,1) will rotate the actor so that its X-axis arrow faces the Z-Up direction.'/>}>
      <Tree content="hello" />
      <Tree content="sub-subtree with children">
        <Tree content="child 1" style={{ color: '#63b1de' }} />
        <Tree content="child 2" style={{ color: '#63b1de' }} />
        <Tree content="child 3" style={{ color: '#63b1de' }} />
      </Tree>
      <Tree content="hello" />
    </Tree>
  </Tree>
);

<Highlight />
